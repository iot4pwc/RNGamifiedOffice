import { RANKING_ACTIONS } from '../constants/ActionTypes';

const initialState = {

}

export const Ranking = (state = initialState, action) => {
	switch (action.type) {
		default: {
			return {
				...state
			};
		}
	}
};
